Company_Name : Ajay Projects
Location_Name : Banglore

#Note: If we want to take this value, i.e if we are running playbook
#for production then we have to pass run time variables while runing the play
#book as #ansible-playbook variables.yaml --extra-variables "env:Prod"